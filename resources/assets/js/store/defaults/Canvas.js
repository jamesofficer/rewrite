import defaultColumn from "./Column";

export default {
    type: "Canvas",
    selected: false,

    columns: [defaultColumn],

    // Properties:
    backgroundColor: "#ffffff",
    padding: {
        top: 20,
        right: 20,
        bottom: 20,
        left: 20
    }
};
